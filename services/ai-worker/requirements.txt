# Core
pika==1.3.2                    # RabbitMQ client
psycopg2-binary==2.9.9         # PostgreSQL driver
SQLAlchemy==2.0.23             # ORM
pydub==0.25.1                  # Audio processing
ffmpeg-python==0.2.0           # Audio concatenation
redis==5.0.1                   # Redis client for progress tracking

# LLM - OpenAI (script generation)
openai>=1.0.0                      # OpenAI API client
tiktoken>=0.8.0                    # Exact token counting for model routing (272K input cap on gpt-5.4-mini)

# TTS - Google Cloud Text-to-Speech (Standard voices)
google-cloud-texttospeech>=2.14.0  # Google Cloud TTS API

# Storage - Google Cloud Storage
google-cloud-storage>=2.14.0       # GCS for audio file storage

# Pub/Sub - decoupled transcript alignment pipeline
google-cloud-pubsub>=2.18.0        # publish alignment requests (generation worker)
flask>=2.3.0                       # HTTP server for the alignment push target
gunicorn>=21.2.0                   # WSGI server for the alignment Cloud Run service

# TTS - Gemini 2.5 Pro (Multi-speaker, podcast-optimized)
google-genai>=1.0.0                # New Gemini API SDK for TTS

# Forced alignment (live transcript timing) — torch/torchaudio are intentionally
# NOT listed here. The Dockerfile installs the CPU-only build from PyTorch's CPU
# index to avoid pulling the ~2GB CUDA wheel. The alignment code imports them
# lazily, so the rest of the worker (and CI) runs fine without them installed.
# numpy IS required at runtime: torch's CPU wheel doesn't pull it (and the CPU
# index can't resolve it), and the aligner decodes audio via numpy. Pinned <2.0
# because torch 2.2.x is built against the NumPy 1.x ABI.
numpy==1.26.4

# Config
pydantic==2.5.2                # Settings validation
pydantic-settings==2.1.0       # Environment settings
python-dotenv==1.0.0           # .env file loading

# HTTP
requests==2.31.0               # HTTP client (for fallback/other APIs)

# Retry logic
tenacity==8.2.3                # Retry with backoff

# Async
aiofiles==23.2.1               # Async file operations

# Logging
structlog==23.2.0              # Structured logging

# Analytics
posthog==3.7.0                 # Product analytics (episode generation outcomes)

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
