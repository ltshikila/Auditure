{
  "name": "auditure-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "services/*"
  ],
  "scripts": {
    "start:mobile": "npx expo start --clear --workspace=apps/mobile-app",
    "start:core-api": "npm run start:dev --workspace=services/core-api",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.0.0" 
  }
}